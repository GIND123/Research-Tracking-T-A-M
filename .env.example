# Copy to .env and fill in. The pipeline runs without a key, using only the
# deterministic and neural tiers; the LLM tiers are skipped and recorded as such.
ANTHROPIC_API_KEY=

# TEKNE_PROPOSER_MODEL=claude-sonnet-5
# TEKNE_VERIFIER_MODEL=claude-haiku-4-5
# TEKNE_ADJUDICATOR_MODEL=claude-opus-5
